# requirements.in
# ================
# Input for pip-compile. Run `make lock` to generate requirements.lock
# with pinned versions and hash verification.
#
# Do NOT pip install from this file directly.
# Use requirements.lock for installs.

click>=8.1
pyyaml>=6.0
rich>=13.0
httpx>=0.27
msal>=1.28
tabulate>=0.9
python-dotenv>=1.0
